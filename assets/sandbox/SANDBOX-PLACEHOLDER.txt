SANDBOX PLACEHOLDER — ЦЕ НЕ РЕАЛЬНИЙ ПРОДУКТ

Цей файл підтверджує, що механізм signed download-токена (create-order
-> webhook -> issueDownloadToken -> download.js) працює end-to-end у
тестовому режимі. Він НЕ є жодним із реальних файлів Starter/Pro/VIP.

Реальні файли пакетів досі лежать лише локально (не задеплоєні на
Netlify) — підключення реального хостингу платних файлів залишається
окремим, нерозв'язаним owner blocker.
